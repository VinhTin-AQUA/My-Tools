export interface SheetStats {
    used_cash: number,
    used_bank: number,
    total_cash: number,
    total_bank: number,
    remaining_cash: number,
    remaining_bank: number,
    total_remaining: number,
}